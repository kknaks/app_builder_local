# Sprint 3: 에이전트 엔진 + 채팅/로그 실시간 통신

> 시작: 2026-03-05
> BE/FE 병렬 작업

## 체크리스트

### S3-BE: 에이전트 엔진
- [ ] Claude Code CLI pty spawn 모듈 (`backend/app/core/agent_runner.py`)
- [ ] ANSI escape code 파싱 (`strip_ansi()`)
- [ ] 프로세스 안전장치 (타임아웃 10분, 좀비 정리, graceful shutdown)
- [ ] 태스크 취소 API (`POST /api/projects/{id}/cancel`)
- [ ] agent_tasks CRUD + 상태 관리 (pending→running→completed/failed/cancelled)
- [ ] 토큰 사용량 파싱 + 비용 추적 (`token_usage` 저장)
- [ ] 비용 조회 API (`GET /api/projects/{id}/cost`)
- [ ] 채팅 WebSocket (`/ws/projects/{id}/chat`)
- [ ] 로그 WebSocket (`/ws/projects/{id}/logs`)
- [ ] chat_messages 저장/조회
- [ ] 에이전트 전환 (switch_agent)
- [ ] pytest 통과
- [ ] ruff lint 통과

### S3-FE: 채팅/로그 UI + 상태 관리
- [x] zustand 스토어 4개 (useProjectStore, useChatStore, useAgentStore, useLogStore)
- [x] 에이전트 탭 바 (채팅 상단, 5개, unread badge)
- [x] 채팅 패널 (에이전트별 1:1, 히스토리, 스크롤 위치 보존)
- [x] 로그 패널 (react-window 가상 스크롤, 에이전트 태그 필터)
- [x] WebSocket 연결 관리 (자동 재연결 지수 백오프)
- [x] 비용 추적 UI (헤더 실시간 토큰 비용 + 에이전트별 breakdown)
- [x] tsc --noEmit 타입 에러 0
- [x] next build 성공

## 완료 조건
- Claude Code CLI spawn → 실행 → 종료 정상 동작
- 채팅 에이전트 전환 동작
- 로그 실시간 스트리밍 확인
- agent_tasks 상태 추적 동작

## 작업 내역
- react-window v2 + 타입 포함 (v2.2.7, @types/react-window 불필요)
- zustand 스토어 4개 생성 (`src/store/projectStore.ts`, `chatStore.ts`, `agentStore.ts`, `logStore.ts`)
- 기존 `src/stores/useProjectStore.ts` → 새 스토어에서 re-export (호환성 유지)
- `AgentTabBar` 컴포넌트: PM/BE/FE/PL/DE 5탭 + 상태 색상(active/idle/waiting/inactive/error) + unread badge
- `ChatPanel` 컴포넌트: 에이전트별 1:1 채팅 UI, WS 메시지 송수신, 히스토리 로드, 스크롤 위치 보존
- `LogPanel` 컴포넌트: react-window v2 `List` 가상 스크롤, 에이전트별 색상 태그, 체크박스 필터, 자동 스크롤
- `useWebSocket` 훅: 자동 재연결 (지수 백오프 1s→2s→4s, 최대 3회), 연결 상태 UI
- `CostTracker` 컴포넌트: 헤더 비용 표시, 10초 폴링, 에이전트별 breakdown 툴팁
- `page.tsx` 업데이트: 헤더 추가 (App Builder 제목 + CostTracker + 설정 버튼)
- `ChatLogPanel` 업데이트: AgentTabBar + ChatPanel + LogPanel 통합

## 완료
- S3-FE 완료: 2026-03-05
- 커밋: 685ed91 (feat(s3-fe): chat/log UI + zustand stores + WebSocket)
